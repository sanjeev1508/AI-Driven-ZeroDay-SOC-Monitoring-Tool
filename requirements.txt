# Windows Event Log access (Agent 1) — Windows only
pywin32>=307

# Web scraping for threat intel (Agent 2)
selenium==4.15.2

# Qdrant vector database client (Agent 2, 3, 4)
qdrant-client==1.16.2

# HTTP client for Ollama API (Agent 2, 3)
requests>=2.31.0
httpx>=0.27.0

# Task scheduling (Agent 2 optional scheduler)
schedule==1.2.0

# Data processing (Agent 1 → 4 CSV pipeline)
pandas>=2.0.0

# Config & validation
python-dotenv==1.0.0
pydantic>=2.9.0

# OpenAI-compatible LLM client (Agent 4 optional)
openai>=1.3.0

# FastAPI backend + file serving
fastapi>=0.104.1
uvicorn>=0.24.0
python-multipart>=0.0.6
aiofiles>=23.0.0

# Optional: LangChain for enhanced LLM analysis in Agent 4
# pip install "langchain>=0.3.0" "langchain-openai>=0.2.0"
